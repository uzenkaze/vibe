# Data files
Asset/*.json
Asset/data/*.json
*.json

# Keep JSON files inside asset and asset-react
!asset/data/*.json
!Asset/data/*.json
!Asset/asset-react/**/*.json
!asset/asset-react/**/*.json

# Keep learn package-lock.json and data.json for CI/CD and sync
!learn/package-lock.json
!learn/data.json

# Keep all deploy_dist files (Vercel serving directory)
!deploy_dist/**

# Node modules
node_modules/
dist/

# Local databases & env
.env
*.db
*.db-journal
dev.db

# Build & Deploy temp directories
temp-deploy/
.gradle/
build/
*.tmp

